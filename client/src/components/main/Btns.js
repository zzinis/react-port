import React from 'react'
import { useRef, useEffect } from 'react';

function Btns() {
    const btnRef = useRef(null);
    let pos = useRef([]);
    return (
        <div>Btns</div>
    )
}

export default Btns